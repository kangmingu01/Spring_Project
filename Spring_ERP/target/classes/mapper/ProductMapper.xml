<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper>
	<insert id="insertProduct">
		<selectKey resultType="int" keyProperty="productId" order="BEFORE">
			select product_seq.nextval from dual
		</selectKey>
		insert into product values(#{productId},#{productCategory},#{productName},#{productPrice},#{deliveryPrice},#{productImg} )
	</insert>
	
	<update id="updateProduct">
		update product set productCategory=#{productCategory},productName=#{productName},productPrice=#{productPrice},deliveryPrice=#{deliveryPrice}
	</update>
	
	<delete id="deleteProduct">
		delete from product where productId=#{productId}
	</delete>
	
	<select id="selectProductAll">
		select * from product
	</select>
	
</mapper>